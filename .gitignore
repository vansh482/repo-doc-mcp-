# Local secrets (API keys, tokens — never push)
.local-secrets/
repo-doc-mcp.yaml

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Node (VS Code extension)
vscode-extension/node_modules/
vscode-extension/out/
vscode-extension/*.vsix

# State files
.repo-doc-state.json
